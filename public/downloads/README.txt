Place your signed release APK here, named exactly:

    app-release-signed.apk

This path is referenced by src/components/DownloadApkMenuItem.jsx as:
    /downloads/app-release-signed.apk

After copying the real APK in, delete this README (or leave it - it won't
be linked to from anywhere) and redeploy.
