# Copy to .env (gitignored) and customize before running.

# The admin login path - keep this secret, don't reuse this example value,
# and never link to it from anywhere in the public UI (blueprint 13.3).
VITE_ADMIN_PATH=/admin-portal-access-secret
